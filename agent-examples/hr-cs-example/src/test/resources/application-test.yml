spring:
  datasource:
    url: jdbc:h2:mem:hr_agent_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true
    target: "2"
  h2:
    console:
      enabled: false

agent:
  platform:
    biz-domain: hr
    biz-module: cs

dashscope:
  api-key:

qdrant:
  host: 127.0.0.1
  port: 6333
