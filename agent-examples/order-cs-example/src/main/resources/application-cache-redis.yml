# 启用 Redis 两级缓存时使用：spring.profiles.active=cache-redis
# 同时将各 @Cached 的 cacheType 改为 CacheType.BOTH
jetcache:
  remote:
    default:
      type: redis.lettuce
      keyConvertor: fastjson2
      valueEncoder: java
      valueDecoder: java
      uri: ${JETCACHE_REDIS_URI:redis://127.0.0.1:6379/}
